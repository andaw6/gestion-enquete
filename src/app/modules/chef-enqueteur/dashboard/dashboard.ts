export interface QuickAction {
  id: number;
  title: string;
  icon: string;
  bgColor: string;
  hoverColor: string;
  action: string;
}

export interface Activity {
  id: number;
  title: string;
  subtitle: string;
  icon: string;
  iconBg: string;
  iconColor: string;
}
